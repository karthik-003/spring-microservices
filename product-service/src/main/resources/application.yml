
spring:
  profiles:
    active: local

---

spring:
  profiles: local

server:
  port: 7001
spring.data.mongodb:
  host: localhost
  port: 27017
  database: product-db

---

spring:
  profiles: docker

server:
  port: 8080